spring:
  datasource:
    url: jdbc:postgresql://localhost:5168/business
    username: yelp-admin
    password: yelp-service
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
server:
  port: 1111
